# Python bytecode / packaging
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Test / lint / type-check caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.tox/

# Virtual environments
.venv/
venv/
env/

# Local project config (written by `python app.py init`; holds machine-
# specific dataset/model paths, not meant to be shared) -- keep the
# checked-in example.
reid.yaml
reid.yml
reid-annotation.yaml

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local AI-assistant tool config (e.g. skill symlinks pointing outside the
# repo to a machine-local skills manager) -- not portable, not shared.
.claude/
.codex/

# Site-specific pipelines contain local dataset/model/device paths. Keep only
# the portable Ultralytics example as a reviewed, versioned starting point.
/backend/pipeline/*
!/backend/pipeline/pipeline.demo.py
