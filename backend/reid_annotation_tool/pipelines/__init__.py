"""Reference pipeline scripts (see reid_annotation_tool/contract.py).

These are examples to copy, never an automatic default: the project config
always names the pipeline it wants, by bundled name or by path.
"""
